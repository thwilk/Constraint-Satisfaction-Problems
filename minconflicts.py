'''2. minconflicts.py – This should run in two modes. a) Plain MinCon-
flicts (MCLS) and b) MinConflicts with Restarts (MCLS-R).
You may have to adjust the MinConflicts algorithm to avoid plateaus or
getting stuck in local depressions.
A sample execution of minconflicts.py should be as below:
python minconflicts.py <INPUT FILE> <OUTPUT FILE>
<MODE FLAG>.
<MODE FLAG> can be either 0 (plain MCLS) or 1 (improved MCLS-R).'''